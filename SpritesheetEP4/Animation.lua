local class = require class
local Anim = class:derive("Animation")

function Anim:new()
   self.fps =
   self.timer =
   self.frame )
   self.num_frames =
   self.xoff =
   self.yoff =
   self.w =
   self.h =
  
end

return Anim