local class = require class
local Anim = class:derive("Animation")

function Anim:new()
   self.fps =
   self.timer = 1 / self.fps
   self.frame )
   self.num_frames =
   self.offset = Vector2()
   self.size = Vector2()
   
   self.h =
  
end

function Anim

return Anim